export default (
    "break case catch const continue else false finally for function if in new null return switch true try typeof var void while" +
    "setMap addText clearText addSprite getGrid getTile tilesWith clearTile setSolids setPushables setBackground map bitmap color tune getFirst getAll width height setLegend onInput afterInput playTune getState"
).split(" ");
